<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Top: fade the banner into the page background. -->
    <item android:bottom="96dp">
        <shape>
            <gradient
                android:angle="270"
                android:startColor="?android:colorBackground"
                android:endColor="@android:color/transparent"
                android:type="linear" />
        </shape>
    </item>

    <!-- Bottom: fade the banner into the page background. -->
    <item android:top="360dp">
        <shape>
            <gradient
                android:angle="270"
                android:startColor="@android:color/transparent"
                android:endColor="?android:colorBackground"
                android:type="linear" />
        </shape>
    </item>

</layer-list>
